name: Keep Alive

on:
  schedule:
    - cron: '*/2 * * * *'  # Runs every 2 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Bot
        run: |
          # Replace with your Render URL
          curl -I https://xolo-57.onrender.com/
